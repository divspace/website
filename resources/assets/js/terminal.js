(function($) {
  $('.output').typed({
    contentType: 'html',
    showCursor: false,
    startDelay: 500,
    strings: [
        'php artisan serve --port=80'
    ],
    typeSpeed: 0
  });
})(jQuery);
